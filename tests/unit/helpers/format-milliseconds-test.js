
import { formatMilliseconds } from 'ember-timetracker/helpers/format-milliseconds';
import { module, test } from 'qunit';

module('Unit | Helper | format milliseconds');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatMilliseconds([42]);
  assert.ok(result);
});

